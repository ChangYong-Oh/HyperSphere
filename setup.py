from setuptools import setup, find_packages

setup(
    name = "HyperSphere",
    author = "ChangYong-Ohs",
    version = 0,
    dependency_links = (),
    install_requires = ['numpy'],
    scripts = [],
    packages=find_packages(),
    )
